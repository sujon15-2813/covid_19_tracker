import 'package:flutter/material.dart';

class WorldStates extends StatelessWidget {
  const WorldStates({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
